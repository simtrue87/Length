# Phase A — 데이터 수집·합성 (로컬, Python 3.10~3.14 호환)
opencv-python>=4.10.0
numpy>=1.26.0
Pillow>=10.0.0
tqdm>=4.66.0
roboflow>=1.3.0  # Roboflow 자동 업로드용
python-dotenv>=1.0.0  # API 키 .env 로드

# Note:
#  - albumentations는 stringzilla가 Python 3.14 휠 없음. Phase A 합성에는 미사용이라 제외.
#    필요시 Python 3.12 venv로 별도 설치하거나 Colab에서 학습 직전 추가.
#  - midv500 패키지(0.2.1)도 구버전이라 Python 3.14 호환 보장 X.
#    MIDV-500 직접 다운로드는 wget/curl 또는 fcakyon/midv500 repo의 download 스크립트 사용.

# Phase B/C — 학습·변환은 Colab에서 진행 (로컬 설치 불필요)
# Colab에서 자동 설치: ultralytics, tensorflow, onnx, onnxruntime
